# 金属厂数据管理系统 — 完整测试清单

> 后端: `http://localhost:8080` | 前端: `npm run dev`
> 数据库: `localhost:3306 / metal_system / root / haoyu2026`

---

## 一、登录与权限

### 1.1 登录
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 打开前端页面 | 自动跳转 `/login` |
| 2 | 输入 `admin` / `admin123` 登录 | 提示"登录成功"，跳转送货记录页 |
| 3 | 查看 header 左侧 | 显示「数据管理系统」 |
| 4 | 查看 header 右侧 | 显示公司下拉框 + 用户名 `admin` + 头像 |

### 1.2 普通用户权限
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 退出登录，用普通用户登录 | — |
| 2 | 左侧菜单 | **没有**「上机物料」「操作日志」「公司管理」「用户管理」 |
| 3 | 手动访问 `/machine-material` | 被重定向到送货记录，提示"没有管理员权限" |

### 1.3 管理员权限
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | admin 登录 | 左侧菜单全部可见（上机物料、操作日志、公司管理、用户管理） |

---

## 二、公司切换

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击 header 公司下拉框 | 显示所有公司列表 |
| 2 | 切换到公司B | 页面刷新，所有表数据切换为公司B的数据 |
| 3 | 新增一条记录 | 记录归属公司B |

**查数据库验证**:
```sql
-- 以送货记录为例
SELECT id, company_id, material_code FROM delivery_record ORDER BY id DESC LIMIT 1;
-- company_id 应该 = 切换后的公司ID
```

---

## 三、送货记录 (delivery-record)

### 3.1 列表与搜索
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 左侧菜单 → 送货记录 | 显示数据列表 + 分页 |
| 2 | 关键词输入 `丝杆` → 搜索 | 过滤出包含"丝杆"的记录 |
| 3 | 类别下拉选择 → 搜索 | 按类别过滤 |
| 4 | 厂房输入 → 搜索 | 按厂房过滤 |
| 5 | 日期范围选择 → 搜索 | 按日期范围过滤 |
| 6 | 点击列头排序 | 排序生效 |

### 3.2 新增
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击"新增" | 对话框打开 |
| 2 | 填入日期 `2026-07-21` | — |
| 3 | 物料编码输入 `TEST-DR-001` | — |
| 4 | 类别 `备件` | — |
| 5 | 物料名称 `测试丝杆` | — |
| 6 | 规格型号 `M8×100` | — |
| 7 | 序列号 `SN20260721` | — |
| 8 | 数量 `10` | — |
| 9 | 单位 `台` | — |
| 10 | 品牌 `FANUC` | — |
| 11 | 产品属性 `新品` | — |
| 12 | 厂房 `A` | — |
| 13 | 送货单号 `SH20260721` | — |
| 14 | 备注 `测试备注` | — |
| 15 | 点击"提交" | 提示"新增成功"，列表刷新 |

### 3.3 语音解析
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 新增对话框 → 语音输入框 | — |
| 2 | 粘贴: `日期2026年7月21日 类别备件 物料名称丝杆 规格型号M8 物料编码ABC 序列号SN001 数量5 单位台 品牌FANUC 产品属性新品 厂房A 送货单号SH001 备注无` | — |
| 3 | 点击"解析" | 提示"已填充 X 个字段"，表单被填入 |
| 4 | 核对表单内容后提交 | 新增成功 |

### 3.4 编辑
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击某条记录"编辑" | 对话框打开，回显数据 |
| 2 | 修改数量为 `20` → 提交 | 提示"修改成功" |

### 3.5 删除
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击某条记录"删除" | 确认框 → 确认后删除成功 |

### 3.6 Excel 导入导出
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击"模板下载" | 下载 Excel 模板 |
| 2 | 填写模板后点击"导入" | 提示导入成功 X 条 |
| 3 | 点击"导出" | 下载 Excel 文件，数据正确 |

---

## 四、原始记录 (original-record)

### 4.1 列表与搜索
同送货记录模式。额外筛选项：班次（白班/夜班）、是否过保。

### 4.2 OCR 图片识别 ⚠️ 重点测试
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 新增 → 滚到表单底部 | 看到「OCR图片识别」区域 |
| 2 | 点击"选择图片"（蓝底白字按钮） | 文件选择器打开 |
| 3 | 选择 `E:\code\System\测试图片\` 中任意一张 | 图片预览出现 |
| 4 | 点击「AI解析」（蓝底白字按钮） | loading 动画，完成后提示 "已填充 X 个字段" |
| 5 | 查看表单 | 识别到的字段被自动填入 |
| 6 | 人工核对 → 提交 | 新增成功 |

**数据库验证**:
```sql
SELECT * FROM original_record ORDER BY id DESC LIMIT 1;
```

### 4.3 语音解析
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 语音输入框粘贴: `日期2026年7月21日 班次白班 厂房A 机台号K25 机型FANUC 诊断人张三 维修人李四 确认人王五 报修时间22时 开始时间23时 结束时间02时 故障现象主轴异响 维修描述更换丝杆 料号2212673-0461 配件名称丝杆 数量1 上机物料号M001 下机物料号M002 备注无` | — |
| 2 | 点击"解析" | 提示已填充字段，日期被正确转为 `2026-07-21`，时间被正确转为 `HH:mm` 格式 |

### 4.4 Excel 导入 — 跨天与工时 ⚠️ 重点测试

准备 Excel 表，填入：

| 日期 | 班次 | 厂房 | 报修时间 | 开始时间 | 结束时间 | 故障现象 |
|------|------|------|----------|----------|----------|----------|
| 2026-07-21 | 白班 | A | 22:00 | 23:00 | 02:00 | 测试 |

> 时间列只填 `HH:mm`，不填日期。工时列留空。

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击"导入"选择该 Excel | 导入成功 |
| 2 | 查数据库验证 | 见下方 |

```sql
SELECT record_date, repair_request_time, start_time, end_time,
       repair_hours, downtime_hours
FROM original_record ORDER BY id DESC LIMIT 1;
```

| 字段 | 期望值 | 说明 |
|------|--------|------|
| record_date | `2026-07-21` | — |
| repair_request_time | `2026-07-21 22:00:00` | 不是1899年 |
| start_time | `2026-07-21 23:00:00` | 不是1899年 |
| end_time | **`2026-07-22 02:00:00`** | 跨天！ |
| repair_hours | **`180`** | 3小时=180分钟 |
| downtime_hours | **`240`** | 4小时=240分钟 |

### 4.5 过保自动查询
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 新增时填入"下机物料号"为已存在的上机物料编码 | 自动回填"上次上机时间"和"是否过保" |

---

## 五、超比统计 (delivery-stats)

### 5.1 百分比 ⚠️ 重点测试

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 新增 → 比例(%) 输入 `15` | — |
| 2 | 填入其他字段 → 提交 | 新增成功 |
| 3 | 回到列表查看比例列 | 显示 **`15%`**（带百分号） |
| 4 | 查询数据库：`SELECT ratio FROM delivery_stats ORDER BY id DESC LIMIT 1;` | 值为 `0.15`（小数） |
| 5 | 编辑该记录 | 比例输入框显示 `15`（不是 0.15） |
| 6 | 改为 `20` → 提交 → 查数据库 | ratio = `0.20` |

### 5.2 自动填充
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 新增 → 料号输入已在156项中存在的编码 | 点击料号输入框旁或下拉选择 |
| 2 | 系统名称、配件名称、单台机用量等 | 自动从156项回填 |
| 3 | 点击"自动填充" | 机台数、送货数量、上机数量、当月返修 自动填入 |

### 5.3 更新当月数据 ⚠️ 重点测试
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 切换到公司A | — |
| 2 | 点击"更新当月数据" | 提示"已更新 N 条记录" |
| 3 | 查数据库验证公司隔离 | 公司A的统计只包含公司A的送货记录 |

---

## 六、结算机台数 (settlement-machine)

### 6.1 百分比
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 新增 → 比例输入 `25` → 提交 | 新增成功 |
| 2 | 列表比例列显示 | **`25%`** |
| 3 | 数据库 `SELECT ratio FROM settlement_machine ORDER BY id DESC LIMIT 1;` | `0.25` |

### 6.2 语音解析
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 语音输入框: `料号TEST 类别备件 配件名称丝杆 单台机用量1 比例25 含税单价100 机型FANUC 结算机台数量50 备注无` | — |
| 2 | 点击"解析" | 字段被填入 |

---

## 七、开机数量 (machine-count)

### 7.1 占比显示
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 查看列表"占比(%)"列 | 显示如 `50.00%`（注意此字段存 0~100 范围，和其他表不同） |

### 7.2 语音解析
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 语音输入框: `机型FANUC 开机数量10 占比50 统计月份2026-07 备注无` → 解析 | 字段被填入 |

---

## 八、156项 (base-material-156)

### 8.1 跨公司唯一性 ⚠️ 重点测试
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 公司A → 新增 → 料号 `DUP-TEST` → 提交 | ✅ 成功 |
| 2 | 公司B → 新增 → 料号 `DUP-TEST` → 提交 | ✅ 应成功（不同公司） |
| 3 | 公司B → 新增 → 料号 `DUP-TEST` → 再次提交 | ❌ 应失败（同公司重复） |

### 8.2 百分比
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 列表比例列 | 显示 `XX%` |
| 2 | 编辑时比例输入框 | 显示百分比数字（非小数） |

---

## 九、管理员专用模块

### 9.1 上机物料 (machine-material)
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | admin 登录 | 左侧菜单有"上机物料" |
| 2 | 普通用户登录 | 左侧菜单**没有**"上机物料" |
| 3 | 完整 CRUD 功能 | 增删改查正常 |

### 9.2 操作日志
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | admin → 操作日志 | 显示所有用户的操作记录 |
| 2 | 刚才的新增/修改/删除操作 | 在日志中可见 |

### 9.3 公司管理
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | admin → 公司管理 | 显示公司列表 |
| 2 | 新增公司 | 成功，header 下拉框出现新公司 |

### 9.4 用户管理
| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | admin → 用户管理 | 显示用户列表 |
| 2 | 修改用户角色 admin↔user | 权限即时生效 |

---

## 十、回归验证 — 语音解析（所有表）

| 表 | 语音解析测试文本 |
|----|----------------|
| 送货记录 | `日期2026年7月21日 类别备件 物料名称丝杆 规格型号M8 物料编码ABC 数量5 品牌FANUC 厂房A` |
| 原始记录 | `日期2026年7月21日 班次白班 厂房A 维修人张三 机台号K25` |
| 上机物料 | `日期2026年7月21日 机台号K25 维修人李四 料号ABC 配件名称丝杆` |
| 超比统计 | `料号ABC 类别备件 单台机用量1 比例15 含税单价100 机台数50` |
| 结算机台数 | `料号ABC 类别备件 单台机用量1 比例25 含税单价100` |
| 机型明细 | `厂房A 机台号K25 机台品牌FANUC` |
| 开机数量 | `机型FANUC 开机数量10 占比50 统计月份2026-07` |
| 物料表 | `类别备件 物料名称丝杆 规格型号M8 物料编码ABC` |

---

## 快速检查卡

| # | 测试项 | 怎么测 | 通过 |
|---|--------|--------|------|
| 1 | 登录 | admin/admin123 登录 | ☐ |
| 2 | 普通用户权限 | user 看不到上机物料/日志/公司/用户 | ☐ |
| 3 | 公司隔离 | 切公司A新增 → 切公司B看不到A的数据 | ☐ |
| 4 | OCR识别 | 原始记录→新增→选图片→AI解析 | ☐ |
| 5 | Excel跨天 | 导入 time=02:00 → end_time=次日02:00 | ☐ |
| 6 | 工时分钟 | repair_hours=180（不是3） | ☐ |
| 7 | 百分比显示 | delivery-stats列表 ratio 显示 15% | ☐ |
| 8 | 百分比存储 | DB中ratio=0.15（不是15） | ☐ |
| 9 | 百分比编辑 | 编辑框显示15（不是0.15） | ☐ |
| 10 | 156唯一性 | 不同公司可同料号，同公司不可 | ☐ |
| 11 | 超比统计隔离 | 公司A更新当月数据不算公司B的单 | ☐ |
| 12 | 语音解析 | 8个表各测一次 | ☐ |
